if Baseless.Needs.Particle then
  CreatePrototype("optimized-particle", {
    life_time = 2,
    pictures = Baseless.AnimationVariations,
    render_layer = Baseless.RenderLayer,
    render_layer_when_on_ground = Baseless.RenderLayer,
  })
end
